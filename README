Interpretador python rodando no lado do servidor.

Ferramentas:
Flask
Python3
virtualenv
pip

Como usar:
$ virtualenv -p /usr/bin/python3 flask
$ source flask/bin/activate
Entre na pasta do py-interpreter-flask
$ pip install -r requirements.txt
$ python app.py
